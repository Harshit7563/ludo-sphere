<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Local dev: HTTP to LAN server (phone on same Wi‑Fi) -->
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
